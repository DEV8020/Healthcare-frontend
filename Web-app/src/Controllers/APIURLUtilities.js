//########################## API Child URL's Keys  ##########################

//API Child URL Keys...
const getAPIChildURLKeys = () => {
  return {
    loginAPIKey: "login/",
  };
};

const APIURLUtilities = {
  getAPIChildURLKeys,
};

export default APIURLUtilities;
